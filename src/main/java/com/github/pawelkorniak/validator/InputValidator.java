package com.github.pawelkorniak.validator;

public class InputValidator implements Validator{

    @Override
    public boolean isValid(String input) {
        //some validations
        boolean result = true;
        return result;
    }
}
